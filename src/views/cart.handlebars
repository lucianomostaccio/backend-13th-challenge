<!-- cart.handlebars -->

<section class="profileSection">
  <ul class="userProfile" id="products-home">
    <li class="profileItem">{{first_name}}</li>
  </ul>
</section>

<h1 class="pageTitle">Products in cart</h1>

<ul class="productList">
  {{#each productsInCart.products}}
    <li class="productTitle">
      {{productId.title}}
      - ${{productId.price}}
      -
      {{productId.description}}
      - quantity:{{quantity}}
      units 
      <form
        class="removeFromCartButton"
        action="/products/{{productId._id}}/remove-from-cart"
        method="POST"
      >
        <button type="submit">Remove from Cart</button>
      </form>
    </li>
  {{/each}}
</ul>

<script src="/js/cart.js"></script>